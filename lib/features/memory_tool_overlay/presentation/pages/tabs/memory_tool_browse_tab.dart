import 'package:flutter/material.dart';

class MemoryToolBrowseTab extends StatelessWidget {
  const MemoryToolBrowseTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('浏览'));
  }
}
